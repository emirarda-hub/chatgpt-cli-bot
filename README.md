# chatgpt-cli-bot
A simple CLI chatbot built with Python and the OpenAI API.

# ChatGPT CLI Bot

A simple command-line chatbot built with Python and the OpenAI API.  
This project allows you to interact with an AI assistant directly from your terminal.

## Features

- Simple CLI interface
- Uses the OpenAI API
- Customizable parameters (temperature, tokens, etc.)
- Lightweight and easy to run

## Requirements

- Python 3.8+
- OpenAI API key
- requests library

## Installation

Clone the repository:

```bash
git clone https://github.com/YOUR_USERNAME/chatgpt-cli-bot.git
cd chatgpt-cli-bot

## Setup API Key

You need an OpenAI API key to run this project.

Set your API key as an environment variable.

Mac / Linux:

```bash
export OPENAI_API_KEY="your_api_key_here"
